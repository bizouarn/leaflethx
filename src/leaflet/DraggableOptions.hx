package leaflet;

typedef DraggableOptions = {
	/**
		The max number of pixels a user can shift the mouse pointer during a click
		for it to be considered a valid click (as opposed to a mouse drag).
	**/
	var clickTolerance : Float;
};